#include<stdio.h>
int main()
{
    int i, sum = 0;
    for(i=10; i<=15; i++)
    {
        sum = sum+1;
        printf("%d for sum=%d\n", i, sum);
    }
    printf("%d for sum = %d", i, sum);
}
